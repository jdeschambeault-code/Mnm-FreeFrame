fastapi==0.141.1
# Pinned transitive: fastapi declares `starlette>=0.46.0` with no upper bound,
# so an unpinned build drifts across starlette MAJOR versions on rebuild alone.
# Starlette is the ASGI layer under the SSE endpoint and the BaseHTTPMiddleware
# stack, so that drift is invisible until it is not. Pinned to the version
# already resolving today; bump it deliberately alongside fastapi.
starlette==1.6.0
uvicorn[standard]==0.52.4
# Pinned transitive: neither anyio nor typing_extensions is a direct
# dependency here, but starlette's anyio import chain (fastapi -> starlette
# -> anyio.abc -> anyio._core._typedattr) needs typing_extensions' `sentinel`
# helper, only added in typing_extensions 4.14+. An unpinned install can
# still resolve an older typing_extensions that satisfies every declared
# range but is missing that symbol, breaking uvicorn's startup import with
# "ImportError: cannot import name 'sentinel' from 'typing_extensions'"
# (confirmed on a real install - a second machine running the exact same
# unpinned requirements.txt resolved a typing_extensions without it, while
# this one didn't). Pinned to the version already resolving/working here.
typing_extensions==4.16.0
sqlalchemy==2.0.53
alembic==1.19.2
psycopg2-binary==2.9.13
pydantic==2.13.5
pydantic-settings==2.15.0
python-jose[cryptography]==3.5.0
bcrypt==4.3.0
python-multipart==0.0.32
boto3==1.43.94
# Pinned transitive: boto3 allows any botocore in >=1.43.56,<1.44.0, and botocore
# is where S3 request signing lives. Silent botocore moves have broken S3
# compatibility here before (SigV4 presigning against non-AWS backends), so this
# is pinned to make those changes arrive as a reviewable PR.
botocore==1.43.94
celery[redis]==5.6.3
redis==5.3.1
httpx==0.28.1
pytest==8.4.2
pytest-asyncio==0.26.0
email-validator==2.3.0
jinja2==3.1.6
kombu==5.6.2
# Official Ayon SDK (same one D:\pinokio\api\Ayon_Planner_v2.3.5.1 uses) for
# services/ayon_service.py - handles GraphQL/REST fallback, pagination, and
# entity shapes correctly instead of hand-rolling raw REST calls.
ayon-python-api==1.2.23
# "Sign in with Google" (routers/auth.py's /auth/google/*): verifies the
# returned id_token's signature against Google's rotating public keys
# (JWKS fetch/cache/rotation/audience/issuer checks all handled), rather
# than hand-rolling that. The authorization redirect and code exchange
# themselves just use the existing httpx dependency - no client library
# needed for those.
google-auth==2.58.0
